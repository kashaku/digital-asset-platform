{
  "name": "asset-chain-workspace",
  "version": "1.0.0",
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../contracts && npm install && cd ../indexer && npm install",
    "dev": "cd frontend && npm run dev",
    "dev:indexer": "cd indexer && npm run dev",
    "compile": "cd contracts && npx hardhat compile",
    "test": "cd contracts && npx hardhat test",
    "node": "cd contracts && npx hardhat node"
  },
  "dependencies": {
    "react-router-dom": "^7.15.0"
  }
}
